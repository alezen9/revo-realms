import { Vector3 } from "three";

export const UP = Object.freeze(new Vector3(0, 1, 0));
export const DOWN = Object.freeze(new Vector3(0, -1, 0));
export const FORWARD = Object.freeze(new Vector3(0, 0, -1));
